name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  # Lets you re-deploy from the Actions tab without pushing a commit.
  workflow_dispatch:

# Read the repo, write a Pages deployment, and mint the OIDC token Pages requires.
permissions:
  contents: read
  pages: write
  id-token: write

# One deploy at a time. `cancel-in-progress: false` so a running deploy is allowed to
# FINISH rather than being killed halfway — a half-published site is worse than a stale one.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          # 24, not 20: `node:sqlite` (used by the dormant server layer's tests) does not
          # exist before 22, and the test script runs across the whole of src/.
          node-version: 24
          cache: npm

      - run: npm ci

      # The site is a shop. It ships only if the money code still passes.
      - name: Typecheck
        run: npx tsc --noEmit

      - name: Lint
        run: npx eslint src

      - name: Test
        run: npm test

      # Advisory, never blocking. It reports what is still unfilled — the WhatsApp number,
      # the NAP, the product photos — every one of which deploys perfectly happily while
      # doing nothing. Failing the deploy over a missing phone number would be its own kind
      # of stupid; not TELLING you is worse. Read it in the log before you announce the site.
      - name: Launch preflight (advisory)
        run: npm run preflight
        continue-on-error: true

      - name: Build static export
        run: npm run build
        env:
          NEXT_PUBLIC_SITE_URL: ${{ vars.NEXT_PUBLIC_SITE_URL }}
          NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION: ${{ vars.NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION }}
          NEXT_PUBLIC_BING_SITE_VERIFICATION: ${{ vars.NEXT_PUBLIC_BING_SITE_VERIFICATION }}

      # `enablement: true` makes the action switch Pages to the "GitHub Actions" source via
      # the API instead of failing when it is still set to "Deploy from a branch".
      #
      # That default is why the first deploy failed: GitHub had Pages on branch mode, ran
      # JEKYLL over the repo root, and published a 1.7 kB stub with the repo name as its
      # title. Jekyll also ignores every folder beginning with `_`, so even had it seen the
      # export it would have dropped all of /_next/ — no CSS, no JS. This flag ends both.
      - uses: actions/configure-pages@v5
        with:
          enablement: true

      - uses: actions/upload-pages-artifact@v3
        with:
          # `output: "export"` writes here. Includes public/.nojekyll, WITHOUT which GitHub
          # runs Jekyll, Jekyll ignores every folder starting with `_`, Next's entire
          # `/_next/` directory is dropped, and the site ships with no CSS and no JS.
          path: ./out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
